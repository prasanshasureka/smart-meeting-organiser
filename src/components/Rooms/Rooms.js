import React from "react";
import Card from "../../UI/Card";

const Rooms = (props) => {
  return (
    <Card className={props.className}>
      <h2>Rooms</h2>
    </Card>
  );
};

export default Rooms;
